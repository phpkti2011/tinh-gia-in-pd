# Báo cáo: TASK-0002.6 — Sanitize hardcoded secrets before baseline commit

| Trường | Giá trị |
|---|---|
| Ngày thực hiện | 2026-05-28 |
| Người thực hiện | Claude (Opus 4.7, 1M context) |
| Trạng thái | ✅ Done — clean baseline committed |
| Loại task | Security cleanup + git init (không refactor engine) |

---

## 1. Bối cảnh

- TASK-0002.5 (init Git + commit baseline) đã được khởi tạo nhưng **dừng giữa chừng** vì phát hiện sẽ commit hardcoded `ADMIN_PASSWORD` thật vào git history — vi phạm rule "tuyệt đối không commit secret".
- User pivot sang TASK-0002.6 để sanitize trước, sau đó mới commit clean baseline.

## 2. Secret đã phát hiện (14 vị trí)

| Loại | Vị trí | Xử lý |
|---|---|---|
| Hardcoded password | `src/App.jsx` × 4 lần | Thay placeholder |
| Hardcoded password | `src/components/smallprint/SettingsPanel.jsx` | Thay placeholder |
| Hardcoded password | `src/components/smallprint/ResultPanel.jsx` (const) | Thay placeholder |
| Hardcoded password | `src/components/largeprint/LPSettingsPanel.jsx` | Thay placeholder |
| Hardcoded password | `src/components/decal/DecalSettingsPanel.jsx` | Thay placeholder |
| Hardcoded password | `src/components/uvdtf/UvdtfSettingsPanel.jsx` | Thay placeholder |
| Hardcoded password | `google-apps-script.js` (root, reference) | `.gitignore` |
| Hardcoded password | `tinh gia ep nhu + tinh cuon nhu_v189.249 (1).html` (legacy) | `.gitignore` |
| Quote password literal | `docs/02-current-audit.md` × 2 | Redact thành `[REDACTED]` |
| Quote password literal | `docs/tasks/TASK-0001-baseline-audit.md` × 1 | Redact thành `<admin-password-redacted>` |
| Vercel OIDC JWT token | `.env.local` | Đã `.gitignore` từ trước (an toàn) |

Ngoài ra, `Tinh-gia-decal 12-7-2024 (3).xlsx` (legacy binary spreadsheet) cũng `.gitignore` cùng nhóm legacy.

## 3. File đã sửa (10 file)

| File | Thay đổi |
|---|---|
| [.gitignore](../../.gitignore) | + 3 entry legacy/reference (google-apps-script.js, file HTML legacy, xlsx legacy) |
| [src/App.jsx](../../src/App.jsx) | 4 vị trí: `'<password>'` → `'TEMP_ADMIN_PASSWORD_PLACEHOLDER'` |
| [src/components/smallprint/SettingsPanel.jsx](../../src/components/smallprint/SettingsPanel.jsx) | 1 vị trí: password check |
| [src/components/smallprint/ResultPanel.jsx](../../src/components/smallprint/ResultPanel.jsx) | const `ADMIN_PASSWORD` |
| [src/components/largeprint/LPSettingsPanel.jsx](../../src/components/largeprint/LPSettingsPanel.jsx) | 1 vị trí |
| [src/components/decal/DecalSettingsPanel.jsx](../../src/components/decal/DecalSettingsPanel.jsx) | 1 vị trí |
| [src/components/uvdtf/UvdtfSettingsPanel.jsx](../../src/components/uvdtf/UvdtfSettingsPanel.jsx) | 1 vị trí |
| [docs/02-current-audit.md](../02-current-audit.md) | 2 vị trí redact |
| [docs/tasks/TASK-0001-baseline-audit.md](TASK-0001-baseline-audit.md) | 1 vị trí redact |

## 4. File đã tạo mới (3 file)

| File | Vai trò |
|---|---|
| [docs/security/SECURITY_NOTES.md](../security/SECURITY_NOTES.md) | Lịch sử secret + cách verify trước khi push |
| [.env.example](../../.env.example) | Template env var cho tương lai (VITE_ADMIN_PASSWORD, VITE_APPS_SCRIPT_URL) — chưa wire vào code |
| [docs/tasks/TASK-0002.6-sanitize-secrets.md](TASK-0002.6-sanitize-secrets.md) | Báo cáo này |

## 5. Cam kết "không đổi logic / UI / bảng giá"

| | Đã làm? |
|---|---|
| Đổi công thức tính giá | ❌ Không (file engine không bị đụng) |
| Đổi cấu trúc tính giá | ❌ Không |
| Đổi giao diện | ❌ Không (chỉ thay literal string trong password check, không sửa JSX/UI) |
| Đổi bảng giá | ❌ Không (file config không bị đụng) |
| Refactor cấu trúc | ❌ Không |
| `npm audit fix --force` | ❌ Không chạy |
| Đụng `.env.local` | ❌ Không |

## 6. Hệ quả phát sinh

| Hệ quả | Ghi chú |
|---|---|
| Admin auth tạm thời chấp nhận chuỗi `TEMP_ADMIN_PASSWORD_PLACEHOLDER` | Lộ rõ là tạm — cần thay bằng Supabase Auth / env var ở task sau |
| `saveConfigToCloud` POST với placeholder → Apps Script (vẫn dùng password thật phía Google) sẽ reject | Cloud sync tạm dừng; local-only mode vẫn hoạt động |
| 3 file legacy còn trên đĩa nhưng không vào git | Dev khác clone repo về sẽ KHÔNG có 3 file này |
| Bundle JS đổi tên hash: `index-58SiIv15.js` → `index-DUoME-sY.js` | Bình thường (content hash phụ thuộc nội dung) |

## 7. Verification (đã chạy)

```
# 1. Grep toàn project cho literal password cũ (thay <OLD_ADMIN_PASSWORD> bằng chuỗi thật, không lưu trong repo)
$ grep -r "<OLD_ADMIN_PASSWORD>" .
→ 0 file matches (sau khi .gitignore loại legacy + sanitize src/docs)

# 2. git check-ignore confirm
$ git check-ignore -v google-apps-script.js "tinh gia ep nhu ... .html" \
                       "Tinh-gia-decal ... .xlsx" .env.local node_modules dist
→ All 6 paths matched .gitignore rules

# 3. npm test
✓ tests/sanity.test.js (2 tests) 2ms
Test Files  1 passed (1)
     Tests  2 passed (2)
  Duration  606ms

# 4. npm run build
vite v5.4.21 building for production...
✓ 55 modules transformed.
dist/assets/index-DUoME-sY.js  319.76 kB │ gzip: 86.09 kB
✓ built in 1.37s

# 5. git diff --cached | grep "<OLD_ADMIN_PASSWORD>"
→ OK: 0 leak in staged diff
```

## 8. Bước tiếp theo

→ Quay lại lộ trình chính: **TASK-0003 — Decal golden tests** ([tasks/TASK-0003-decal-golden-tests.md](../../tasks/TASK-0003-decal-golden-tests.md)).

Khuyến nghị mạnh (chưa làm vì ngoài scope):
1. **Rotate password thật trên Apps Script deployed** + cập nhật file `google-apps-script.js` local. Password cũ đã lộ trong build dist cũ (trước TASK-0002.6) — coi như "đã bị compromised".
2. Wire `import.meta.env.VITE_ADMIN_PASSWORD` vào src/ thay vì để placeholder, ngay khi có thời gian (nhanh, không cần Supabase).
