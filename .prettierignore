node_modules
dist
coverage
.vercel
package-lock.json

# Docs giữ nguyên format hiện tại — không format hàng loạt
docs

# SQL files — Prettier không xử lý SQL native
*.sql

# Legacy / gitignored files (nếu còn trên disk dev)
google-apps-script.js
*.html

# Markdown trong root cũng giữ nguyên
*.md

# P3-FMT: scope format:check chỉ validate src/ + tests/ (JS/JSX).
# Các file dưới đây không nằm trong scope format — bỏ qua để CI pass.
# Sau này nếu muốn format chúng, gỡ ignore + chạy prettier --write riêng.
.github/workflows/*.yml
.prettierrc
eslint.config.js
package.json
postcss.config.js
tailwind.config.js
vite.config.js
src/index.css
